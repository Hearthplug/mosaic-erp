Mosaic ERP is installed with {{ .Values.replicaCount }} application replicas and external PostgreSQL.
The migration Job uses {{ .Values.database.migrationSecretName }}; runtime pods use {{ .Values.database.secretName }}.
{{- if .Values.ingress.enabled }}
Open https://{{ (index .Values.ingress.hosts 0).host }}/
{{- else }}
kubectl port-forward svc/{{ include "mosaic.fullname" . }} 8000:{{ .Values.service.port }}
{{- end }}
