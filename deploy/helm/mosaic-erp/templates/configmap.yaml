apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mosaic.fullname" . }}
  labels:
    {{- include "mosaic.labels" . | nindent 4 }}
data:
  MOSAIC_HOST: "0.0.0.0"
  MOSAIC_DB_PATH: "/data/mosaic.db"
  MOSAIC_RATE_LIMIT_RPM: {{ .Values.config.rateLimitRpm | quote }}
