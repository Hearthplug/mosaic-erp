apiVersion: v1
kind: Service
metadata:
  name: {{ include "mosaic.fullname" . }}
  labels:
    {{- include "mosaic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mosaic.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
