apiVersion: v1
kind: Service
metadata:
  name: mosaic-erp
  labels: {app.kubernetes.io/name: mosaic-erp}
spec:
  selector: {app.kubernetes.io/name: mosaic-erp}
  ports:
    - {name: http, port: 80, targetPort: http}
