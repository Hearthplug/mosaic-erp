# Copy to .env.dev and replace all placeholders with generated local values.
POSTGRES_DB=mosaic
POSTGRES_USER=mosaic
POSTGRES_PASSWORD=REPLACE_ME
MOSAIC_DATABASE_URL=postgresql://mosaic:REPLACE_ME@postgres:5432/mosaic?sslmode=disable
