MIGRATION_SQLITE_SCHEMA=r'''
CREATE TABLE import_batches(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,kind TEXT NOT NULL,pack_version INTEGER NOT NULL,source_system TEXT NOT NULL,source_hash TEXT NOT NULL,row_count INTEGER NOT NULL,control_total_minor INTEGER NOT NULL,status TEXT NOT NULL CHECK(status IN ('validated','rejected','imported','reconciled','variance','rolled_back')),errors_json TEXT NOT NULL,rows_json TEXT NOT NULL DEFAULT '[]',applied_json TEXT NOT NULL DEFAULT '[]',created_by TEXT NOT NULL,created_at TEXT NOT NULL,applied_at TEXT,rolled_back_at TEXT,UNIQUE(workspace_id,kind,source_hash));
CREATE TABLE import_entity_map(workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,kind TEXT NOT NULL,external_id TEXT NOT NULL,entity_id TEXT NOT NULL,batch_id TEXT NOT NULL REFERENCES import_batches(id),PRIMARY KEY(workspace_id,kind,external_id));
'''
