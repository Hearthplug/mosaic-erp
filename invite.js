const $=s=>document.querySelector(s),token=new URLSearchParams(location.search).get('token');let invite;fetch('/api/invitations/inspect?token='+encodeURIComponent(token||'')).then(async r=>{let x=await r.json();if(!r.ok)throw Error(x.error);invite=x.invitation;$('#title').textContent='You are invited';$('#detail').textContent=invite.email+' · '+invite.role}).catch(e=>{$('#accept').hidden=true;$('#error').textContent=e.message});$('#accept').onsubmit=async e=>{e.preventDefault();let r=await fetch('/api/invitations/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password:$('#password').value})}),x=await r.json();if(!r.ok)return $('#error').textContent=x.error;MosaicAuth.set(x,true);location.href='/operations'};
